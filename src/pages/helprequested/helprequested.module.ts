import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { HelprequestedPage } from './helprequested';

@NgModule({
  declarations: [
    HelprequestedPage,
  ],
  imports: [
    IonicPageModule.forChild(HelprequestedPage),
  ],
})
export class HelprequestedPageModule {}
